name: Release JSR

on:
  push:
    tags: ['v*']

permissions: {}

jobs:
  release-jsr:
    uses: lumirelle/workflows/.github/workflows/release-jsr.yml@v6
    permissions:
      contents: read
      id-token: write
    with:
      publish: true
