name: Release Commit Dogfood

on:
  push:
    branches: ['**']
    tags-ignore: ['v*']
  pull_request: {}

permissions: {}

jobs:
  release-commit:
    uses: lumirelle/workflows/.github/workflows/release-jsr.yml@v3
    with:
      publish: false
