name: Release Commit

on:
  push:
    tags-ignore: ['v*']
  pull_request: {}

permissions: {}

jobs:
  release-commit:
    uses: lumirelle/workflows/.github/workflows/release-commit.yml@v2
    with:
      compact: true
