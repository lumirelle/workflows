name: autofix.ci

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  autofix:
    uses: lumirelle/workflows/.github/workflows/ci-autofix.yml@v4
    permissions:
      contents: read
