name: CI Coverage

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  coverage:
    uses: lumirelle/workflows/.github/workflows/ci-coverage.yml@v9
    permissions:
      id-token: write
