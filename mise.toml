[tools]
# Environments
node = 'latest'
'github:nubjs/nub' = 'latest'
# Quality
hk = 'latest'
pkl = 'latest'
stylua = 'latest'

[deps.nub]
description = 'Automatically install Node.js dependencies via nub.'
sources = ['package.json', 'nub.lock']
outputs = ['node_modules/']
run = 'nub install'
auto = true

[env]
_.path = ['{{config_root}}/node_modules/.bin']

[tasks.build]
description = 'A mocked build task.'
run = 'echo "Build completed!"'

[tasks.check]
description = 'Run all checks.'
usage = '''
flag "-s --staged" help="Run on staged files only without stashing unstaged changes"
'''
run = '''
hk check {{ "--staged" if usage.staged else "--all" }}
'''
depends = 'build'

[tasks.fix]
description = 'Fix all check issues.'
usage = '''
flag "-s --staged" help="Run on staged files only without stashing unstaged changes"
'''
run = '''
hk fix {{ "--staged" if usage.staged else "--all" }}
'''
depends = 'build'

[tasks.test]
description = 'A mocked test task.'
run = 'echo "Tests passed!"'

[tasks.prerelease]
description = 'Prepare for release.'
# All tasks in parallel.
run = { tasks = ['check', 'test'] }

[tasks.release]
description = 'Bump the version and push new version of this workflow with a Git tag.'
depends = 'prerelease'
run = 'bumpp'
raw = true
depends_post = 'postrelease'

[tasks.changelog]
description = 'Generate GitHub changelog.'
run = 'changelogithub'

[tasks.postrelease]
description = 'Sync version branch with main.'
env = { VERSION_BRANCH = 'v9' }
run = '''
git update-ref refs/heads/$VERSION_BRANCH refs/heads/main
git push origin $VERSION_BRANCH --force
'''

[tasks.publish]
description = 'A mocked publish task.'
run = 'echo "Published!"'

[tasks.'publish:commit']
description = 'A mocked publish commit task.'
run = 'echo "Commit published!"'
