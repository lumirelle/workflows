name: Release JSR Dogfooding

on:
  push:
    tags: ['v*']

permissions: {}

jobs:
  release-jsr:
    uses: lumirelle/workflows/.github/workflows/release-jsr.yml@main
    permissions:
      contents: read
      id-token: write
    with:
      publish: false
