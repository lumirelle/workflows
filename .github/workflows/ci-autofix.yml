name: autofix.ci

on:
  workflow_call:
    inputs:
      primary-os:
        required: false
        type: string
        default: ubuntu-slim

      fix:
        required: false
        type: string
        default: mise run fix

      debug:
        required: false
        type: boolean
        default: false

jobs:
  autofix-bun:
    runs-on: ${{ inputs.primary-os }}
    permissions:
      contents: read
    steps:
      - name: Setup Mise
        uses: lumirelle/workflows/setup-mise@v8
        with:
          debug: ${{ inputs.debug }}

      - name: Fix
        run: ${{ inputs.fix }}

      - name: Commit
        uses: autofix-ci/action@7a166d7532b277f34e16238930461bf77f9d7ed8 # v1.3.3
