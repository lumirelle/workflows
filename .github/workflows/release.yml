name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: lumirelle/workflows/release.yml@v1
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
