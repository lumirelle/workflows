name: Release Commit Dogfood

on:
  push:
    branches: ['**']
    tags-ignore: ['v*']
  pull_request: {}

permissions: {}

jobs:
  release-commit:
    uses: lumirelle/workflows/.github/workflows/release-commit.yml@main
    with:
      compact: true
      publish: false
