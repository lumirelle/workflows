name: Release Commit Dogfooding

on: [push, pull_request]

permissions: {}

jobs:
  release-commit:
    uses: lumirelle/workflows/.github/workflows/release-commit.yml@main
    with:
      cache-preset: 'nub,hk'
      debug: true
